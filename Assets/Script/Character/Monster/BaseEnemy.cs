using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Vacuname
{
    public class BaseEnemy : MonoBehaviour
    {


/*        private void Start()
        {
            behaviorTree = new BehaviorTree(this);
            behaviorTree.root = new Selector(new List<BehaviorNode> {
            new Sequence(new List<BehaviorNode> { new Attack(this) }),
            new Sequence(new List<BehaviorNode> { new Heal(this) })
        });
        }

        private void Update()
        {
            behaviorTree.Execute();
        }

        public void AttackPlayer()
        {
            Debug.Log("Enemy attacks player");
            health -= 10;
        }

        public bool IsPlayerNearby()
        {
            // check if player is within a certain distance
            return true;
        }

        public bool IsEnemyHurt()
        {
            // check if enemy's health is below a certain threshold
            return health < 50;
        }*/
    }


}
